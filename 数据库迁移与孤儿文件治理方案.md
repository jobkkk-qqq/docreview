# 旧数据库迁移与孤儿文件治理 — 完整解决方案

> 版本：V1.0（评审稿）
> 面向：数据库 schema 演进业务升级（如新增分类字段）时，把服务器旧库安全迁移到新结构；
> 以及异地部署时，让磁盘已有文件与数据库正确关联，杜绝数据丢失、库表混乱与孤儿文件。

---

## 一、问题与目标

### 1.1 两类典型场景

| 场景 | 触发时机 | 风险 |
|---|---|---|
| **A. Schema 演进迁移** | 新版新增字段/表（如 v5 三级分类 `department_id`/`doc_level`） | 旧库启动报错、字段缺失、业务不兼容 |
| **B. 异地交付迁移** | 把旧 `doc-repo` 文件拷进新部署包，但库是空库 | **孤儿文件**：磁盘有文件、库无记录 → 全部不可见，等于数据"丢失" |

### 1.2 目标

1. **不丢数据**：历史文档、用户、角色、分类、权限、审计日志完整保留；
2. **不混乱**：schema 升级幂等、有序、可回滚，库与磁盘文件一一对应；
3. **无孤儿文件**：迁移后能双向校验 `数据库记录 ⇄ 磁盘文件`，孤儿可识别、可找回、可清理；
4. **低门槛**：服务器上由一键脚本/内置功能执行，无需用户懂 SQL。

---

## 二、现状盘点（已验证的能力与局限）

### 2.1 已有能力

| 机制 | 文件 | 能力 |
|---|---|---|
| Schema 版本号迁移 | `backend/app/core/auto_migrate.py` | 启动时一次轻量 SELECT 读取 `system_configs.schema_version`；落后才按 v1→v5 顺序 `ALTER` 补列；幂等；事务内 DDL |
| 启动 doc-repo 扫描 | `backend/app/services/repo_scanner.py` | 后台线程，对**库中已有记录**的文档修复路径、按 UUID 关联同名 PDF（`find_existing_pdf` 三级查找） |

### 2.2 已核实的局限

- `auto_migrate` 只做 **DDL 补列**，不识别、不导入磁盘孤儿文件；
- `repo_scanner` 只处理"库中已有记录"的文档，**从不扫描磁盘发现新文件**（`select(Document)` 后遍历记录，磁盘上无记录的文件被忽略）；
- **孤儿文件根因** = 只搬运文件、不同步搬运数据库元数据。

### 2.3 当前交付包实测数据（现状铁证）

以当前 `DocReview-Windows` 为例，执行双向比对后：

- `documents` 表记录：**0 条**（空库）
- 磁盘文档文件（排除 `.lo_profile`）：**115 个**，归并为 **103 个文档 UUID**
  - 12 个 = Office 源文件 + 同名 PDF 预览
  - 91 个 = 纯 PDF 文档
- **孤儿占比：103/103 = 100%**（全部无库记录，系统上完全不可见）
- 另有 **50 个 `.lo_profile` 垃圾文件**（LibreOffice 转换残留，非文档）
- 生产库当前 `schema_version=4`，而 `CURRENT_SCHEMA_VERSION=5`，首次启动会补 v5（`department_id`/`doc_level` 及索引），这印证了**启动自动迁移机制可用**。

---

## 三、方案架构

采用 **"升级数据库 → 关联文件 → 治理孤儿"三阶段流水线**，每一阶段独立、可校验、可回滚。

```
┌────────────────────────────────────────────────────────────────┐
│  迁移前自动备份（docreview.db + doc-repo 快照 + 日志/校验和）      │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 阶段一：数据库 Schema 升级（复用 auto_migrate，v1→v5 有序执行）   │
│   · 事务内 DDL，幂等，失败回滚并保留备份                          │
│   · 升级后 schema_version 写为最新                              │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 阶段二：DB记录 ⇄ 磁盘文件 双向比对（新增核心）                     │
│   1) 磁盘文件索引（排除 .lo_profile 等垃圾目录）                   │
│   2) 数据库文件路径索引（file_path + pdf_path）                   │
│   3) 归并判定：按 UUID 归组（office+同名pdf=1文档）                │
│   4) 输出三分类：                                  正常 / 孤儿 / 悬空 │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 阶段三：孤儿治理（策略可选，先报告后处置）                         │
│   A. 只出报告（默认安全）——列出孤儿、悬空、垃圾，不自动改数据        │
│   B. 找回关联 ——孤儿 pdf 若与库中 office 同 UUID → 补为预览       │
│   C. 清理垃圾 ——删除 .lo_profile 残留、临时文件                    │
│   D. 导入孤儿 ——孤儿文件按"待发布"补建元数据入库存档（需人工确认）    │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│  迁移报告落盘 + 写审计日志（成功N / 匹配N / 孤儿N / 悬空N / 清理N）  │
└────────────────────────────────────────────────────────────────┘
```

---

## 四、分阶段详细设计

### 阶段一：Schema 升级（数据库字段迁移）

**现状已具备，需补三点加固：**

1. **统一迁移版本**：补齐 `_migrate_v5` 后 `CURRENT_SCHEMA_VERSION=5` 已生效；后续每次新增字段必须递增版本号并新增对应 `_migrate_vN()`，规范化命名。
2. **迁移前自动快照**：进入迁移前先复制 `backend/docreview.db` → `backend/backup/docreview.db.bak-YYYYMMDD-HHMMSS`，并记录库文件与 doc-repo 的关键统计（文档数、表数、版本号）到日志，便于回滚与核对。
3. **迁移失败兜底**：DDL 在事务内；任一 `_migrate_vN` 抛异常则整体回滚并保留快照，启动流程不中断（记录 `[AutoMigrate] 迁移失败，已回滚`）。

> 已在生产包验证：空库 + `schema_version=4` 的库，首次启动会成功升到 v5，说明该管线闭环可用。

### 阶段二：库记录 ⇄ 磁盘文件 双向比对（新增核心能力）

新增服务 `repo_audit.py`，提供 `audit_repo_vs_db(repo_path) -> AuditReport`：

- **磁盘侧**：遍历 doc-repo，剔除 `.lo_profile`、隐藏/临时文件；按 UUID 归组，识别"office 源 + 同名 pdf 预览"配对。
- **库侧**：收集所有 `documents.file_path`、`documents.pdf_path` 的 UUID。
- **判定**：
  - 正常：磁盘 UUID 存在于库中任意路径；
  - **孤儿文件**：磁盘有文件、库中无任何记录引用；
  - **悬空记录**：库中有记录、磁盘缺文件（数据实丢，需重点提示）。

### 阶段三：孤儿治理（策略化，可配置）

| 策略 | 行为 | 适用 |
|---|---|---|
| **A 报告（默认）** | 仅生成报告，不改任何数据 | 上线初期、保守运维 |
| **B 找回关联** | 孤儿 `.pdf` 若与库中同名 office 文档 UUID 一致 → 补 `pdf_path` | 修复"PDF 未被关联"的存量 |
| **C 清理垃圾** | 删除 `.lo_profile`、`*.tmp` 等非文档残留 | 释放空间、去脏 |
| **D 导入孤儿** | 对无记录的磁盘文档按"待发布/无分类"补建记录入库存档（**保留原文件名、大小、哈希**，标题可空，需管理员在列表补全元数据后发布） | 用户只带文件、没带库的异地恢复 |

> 原则：**策略 A/B 自动安全执行；C/D 需人工确认后执行**，杜绝误删。

### 工具形态（需你评估，见第六节）

推荐 **"内置迁移与校验"** 形态：

- 启动时：`auto_migrate`（schema 升级）+ 后台 `repo_scanner`（正常关联）不变；
- 提供一个管理入口《数据迁移/文件校验》：一键执行"副本+比对+报告"，运维人员从界面/日志查看三类结果并手动触发 C/D 策略；
- 提供独立 `迁移工具.exe`（`--migrate --audit --report` 参数）供无法打开管理界面的场景使用。

---

## 五、安全与回滚保障

1. **先备份再迁移**：任何写操作前自动快照，操作可逆；
2. **幂等**：schema 版本号保证不重复迁移；比对/导入打 UUID 去重键；
3. **事务**：DDL 与导入分别在独立事务中，失败可回滚；
4. **迁移日志**：`docreview.log` 追加 `[DB Migration]` / `[Repo Audit]` 标签，记录前后统计与耗时；
5. **审计留痕**：孤儿导入、垃圾清理等写操作记入 `audit_logs`（operator=system）；
6. **人工确认兜底**：C/D 策略二次确认，防止误操作。

### 风险评估与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| 迁移升级失败 | 库结构中途异常 | 事务回滚 + 备份快照 + 幂等重试 |
| 孤儿 pdf 被误作为 office 预览 | 关联错乱 | 严格按"同名 UUID"匹配，逐条入报告由人确认 |
| 清理 `.lo_profile` 误删 | 无业务损失 | 仅清理已知垃圾目录模式，白名单化 |
| 并行迁移/服务器并发写入 | 数据竞争 | 迁移期间锁定导入态，禁止上传 |
| 只带文件不带旧库 | 全孤儿 | 阶段三策略 D 提供"找回导入"闭环 |

---

## 六、请评估的决策点

1. **工具形态**（三选一）
   - A. 内置管理《数据迁移/文件校验》页 + 启动自动迁移（推荐，运维最省心）
   - B. 独立 `迁移工具.exe`（参数化 CLI，适合无界面/脚本化）
   - C. 仅 `.bat` + Python 脚本（轻量，但依赖 Python 环境）

2. **孤儿治理默认策略**
   - A. 默认只出报告，人工处置（最安全，推荐）
   - B. 自动执行 A+B（报告 + 找回关联），C/D 仍需确认

3. **孤儿导入（策略 D）的归属**
   - 导入为"待发布/无分类"草稿，由管理员补全元数据后正式归档
   - 或仅生成清单供人工决定（不自动建记录）

4. **迁移触发方式**
   - 启动全自动（无感，推荐）
   - 手动触发（提供明确入口，更可控）

5. **本次是否立即落地**
   - 若确认，我按"阶段一加固 → 阶段二比对 → 阶段三策略 → 工具入口"顺序交付，先在**副本库**上做端到端验证，再回写生产包。

---

> 备注：当前生产包 `doc-repo` 中的 103 个孤儿文件，本质是"文件已拷入、旧库未随迁"。若要完整保留，需同时把服务器旧 `backend/docreview.db` 一并放入部署包；本方案让这个过程**更安全、可校验、可回滚**。